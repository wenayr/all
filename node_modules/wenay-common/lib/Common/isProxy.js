"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isProxy = isProxy;
const m = new WeakSet();
set();
function set() {
    const proxy = Proxy;
    Proxy = new Proxy(proxy, {
        construct(target, argArray, newTarget) {
            const p = new proxy(...argArray);
            m.add(p);
            return p;
        }
    });
}
function isProxy(a) { return m.has(a); }
