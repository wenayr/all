import type { AxiosStatic } from "axios/index";
export type tFtt = {
    apiKey: string;
    axios: AxiosStatic;
    apiSecret: string;
};
export declare const binanceHeader: (apiKey: string) => {
    headers: {
        'Content-type': string;
        'X-MBX-APIKEY': string;
    };
};
export type tFF = {
    type: "IP" | "UID" | string;
    wight: number;
    timeMs: number;
    max: number;
};
export type standard<T extends (...args: any) => any> = {
    wight: tFF | tFF[];
    function: T;
};
type tSimpleFunc = (...arg: any) => any;
export type tSimpleFunc2<T> = (arg: T) => (standard<tSimpleFunc>);
export declare abstract class aBinanceDataFunc {
    [ket: string]: tSimpleFunc2<tFtt>;
}
export {};
